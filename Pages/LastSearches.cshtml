@page
@model LataPrzestepne.Pages.OstatnioSzukaneModel
@{
    
<h1>Ostatnie wyszukiwania</h1>
    @foreach (var item in Model.Osoby)
        {
            <span>
                @item.Result
            </span>
            //<span>
            //    @item.Name, @item.Year - rok

            //    @if (@item.RokPrzestepny())
            //    {
            //        <span>przestępny</span>
            //    }
            //    else
            //    {
            //        <span>nieprzestępny</span>
            //    }

            //</span>

            <br />
        }
}
